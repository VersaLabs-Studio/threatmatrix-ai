# Phase 3: Alert Console + AI Analyst E2E Testing Plan

> **Date:** 2026-04-06
> **Session:** Day 20-21
> **Version:** v0.6.1 → v0.6.3 (target)

---

## Objective

Verify end-to-end functionality of the Alert Console and AI Analyst modules with real attack data from Phase 2, ensuring AI narratives are populated, streaming responses work correctly, and the full detection→analysis pipeline is demo-ready.

---

## Completed Tasks ✅

### Task 1.1-1.3: Alert Console E2E Verification ✅
- [x] Page loads without errors
- [x] Alert cards render with expanded details (ML scores + AI narrative)
- [x] Category filter works correctly
- [x] Alert detail page loads at `/alerts/{id}` (standalone page, not drawer)
- [x] AI narrative displays correctly
- [x] ML scores display correctly (composite, IF, RF, AE)
- [x] Backend rebuilt and deployed with ML score fields in API response

### Task 1.5: Alert Status Update — Backend + Frontend ✅
- [x] Backend `VALID_TRANSITIONS` updated to allow flexible status changes
- [x] Added `reopened` status to the status workflow
- [x] Fixed UUID type mismatch in `get_alert_by_id` and `update_alert_status`
- [x] Backend accepts `resolution_note` for resolved, false_positive, and reopened
- [x] Detail page shows dynamic action buttons based on current status
- [x] Detail page has resolution note modal for status changes requiring notes
- [x] List cards show dynamic action buttons per status
- [x] Status filter includes all statuses: open, acknowledged, investigating, resolved, false_positive, reopened

---

## Pending Verification Tasks

### Task 1.4: API Endpoint Verification

**Run on VPS (after rebuilding backend):**
```bash
git pull
docker compose build backend --no-cache && docker compose up -d backend
curl -s "http://187.124.45.161:8000/api/v1/alerts/TM-20260405180554-1C4FCE19" | python3 -m json.tool
```

**Verify the response includes:**
- [ ] `ai_narrative` field with content (long text, not null)
- [ ] `ioc_enrichment` object with `has_match` field
- [ ] `flow_ids` array (may be empty `[]`)
- [ ] All ML score fields: `composite_score`, `if_score`, `rf_score`, `ae_score`
- [ ] No 500 Internal Server Error

### Task 1.5: Visual Confirmation of Status Update

**Browser Testing Steps:**
1. Navigate to `/alerts`
2. Find an alert with status "open"
3. Click "Acknowledge" → status changes to "acknowledged"
4. Refresh → status persists
5. Navigate to `/alerts/{id}` for that alert
6. Click "INVESTIGATE" → status changes to "investigating"
7. Click "RESOLVE" → resolution note modal appears
8. Enter note and confirm → status changes to "resolved"
9. Refresh → status persists as "resolved"
10. Click "REOPEN" → status changes to "reopened"
11. Refresh → status persists as "reopened"

**API Verification:**
```bash
curl -s "http://187.124.45.161:8000/api/v1/alerts/TM-20260405180554-1C4FCE19" | python3 -c "
import sys, json
d = json.load(sys.stdin)
print('Status:', d.get('status'))
print('Resolved at:', d.get('resolved_at'))
print('Resolution note:', d.get('resolution_note'))
print('AI narrative length:', len(d.get('ai_narrative') or ''))
print('IOC enrichment:', d.get('ioc_enrichment'))
print('Flow IDs:', d.get('flow_ids'))
"
```

### Task 2: AI Analyst E2E Verification

- [ ] Navigate to http://localhost:3000/ai-analyst
- [ ] Verify chat interface loads
- [ ] Send query: "Explain the latest port scan alert"
- [ ] Observe streaming response (typing effect)
- [ ] Record response time
- [ ] Verify response quality (technical accuracy, no hallucinations)

### Task 3: WebSocket Connection Verification

- [ ] Open browser DevTools → Network tab → Filter by WS
- [ ] Verify connection to `ws://187.124.45.161:8000/api/v1/ws/`
- [ ] Keep `/alerts` page open
- [ ] Trigger new alert (run port scan or wait for live traffic)
- [ ] Verify new alert appears without page refresh

### Task 4: Remaining Phase 2 Tasks

#### 4.1 Detection Latency Logging
**File:** `backend/ml/inference/worker.py`
- [ ] Add timestamp when flow is received from Redis
- [ ] Add timestamp when ML scoring completes
- [ ] Add timestamp when alert is created
- [ ] Calculate and log latency: flow_reception → alert_creation
- [ ] Publish latency metrics to Redis for frontend display

#### 4.2 Diverse Attack Simulation
**Scripts:** `scripts/attack_simulation/`
- [ ] Run `02_ddos_simulation.sh` — verify CRITICAL alert
- [ ] Run `03_dns_tunnel.py` — verify MEDIUM alert, category "dns_tunnel"
- [ ] Run `04_brute_force.sh` — verify HIGH alert, category "brute_force"
- [ ] Run `05_normal_traffic.sh` — verify ZERO false positives
- [ ] Document results for each attack type

#### 4.3 Redis Security Hardening
**File:** `docker-compose.yml`
- [x] Verify Redis bound to 127.0.0.1 only: `"127.0.0.1:6379:6379"`
- [ ] Verify no external access: `docker compose exec redis redis-cli INFO replication`

---

## Success Criteria

| Criterion | Target | Actual | Pass/Fail |
|-----------|--------|--------|-----------|
| Alert Console loads | Yes | ✅ | ✅ |
| Alert detail shows AI narrative | Populated | ✅ | ✅ |
| ML scores display correctly | All 4 scores visible | ✅ | ✅ |
| Category filter works | Filters correctly | ✅ | ✅ |
| Status update works | open → acknowledged → investigating → resolved | ✅ | ✅ |
| Resolution note modal works | Note saved on status change | ✅ | ✅ |
| Reopen status works | resolved/reopened → open | ✅ | ✅ |
| API returns IOC enrichment | `ioc_enrichment` object | ___ | ___ |
| API returns flow_ids | Array present | ___ | ___ |
| Status persists after refresh | Yes | ___ | ___ |
| AI Analyst page loads | Yes | ___ | ___ |
| Streaming chat works | SSE streaming | ___ | ___ |
| Response latency | <10s | ___s | ___ |
| Response quality | ≥4/5 | ___/5 | ___ |
| WebSocket connection | Connected | ___ | ___ |
| Real-time alert push | <5s latency | ___s | ___ |

---

## Execution Order

1. **Task 1.4:** API endpoint verification (5 min)
2. **Task 1.5:** Visual confirmation of status update (5 min)
3. **Task 2:** AI Analyst E2E testing (15 min)
4. **Task 3:** WebSocket connection verification (10 min)
5. **Task 4.1:** Detection latency logging (20 min)
6. **Task 4.2:** Diverse attack simulation (30 min)
7. **Task 4.3:** Redis security verification (5 min)
8. **Documentation and reporting (15 min)

**Estimated Total:** ~105 minutes

---

## Risk Mitigation

| Risk | Mitigation |
|------|-----------|
| AI narrative not populated | Run `/api/v1/llm/analyze-alert/{id}` manually |
| Streaming fails | Check LLM Gateway logs, verify OpenRouter connectivity |
| WebSocket not connecting | Check DEV_MODE settings, verify token in localStorage |
| Attack scripts fail | Use PCAP upload fallback from `pcaps/demo/` |
| VPS components idle | `docker-compose restart` on affected services |

---

## Current Status

**Backend Fixes Applied:**
- `VALID_TRANSITIONS`: Flexible transitions with `reopened` status
- `get_alert_by_id`: UUID type mismatch fixed
- `update_alert_status`: UUID type mismatch fixed, resolution_note accepted for resolved/false_positive/reopened
- `STATUS_PERMISSIONS`: Updated for all statuses

**Frontend Fixes Applied:**
- Detail page: Dynamic action buttons based on current status
- Detail page: Resolution note modal for status changes requiring notes
- List page: Dynamic action buttons per status
- List page: Status filter includes all statuses

**Next:** Rebuild backend on VPS, verify API works, then proceed with Task 1.4 and 1.5 verification.

---

_Phase 3 Execution Plan — Alert Console + AI Analyst E2E Testing_
_Target: v0.6.3 with verified E2E detection→analysis pipeline_
