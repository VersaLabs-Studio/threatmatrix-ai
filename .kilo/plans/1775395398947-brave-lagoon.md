# Phase 3: Alert Console + AI Analyst E2E Testing Plan

> **Date:** 2026-04-05
> **Session:** Day 20
> **Version:** v0.6.1 → v0.6.2 (target)

---

## Objective

Verify end-to-end functionality of the Alert Console and AI Analyst modules with real attack data from Phase 2, ensuring AI narratives are populated, streaming responses work correctly, and the full detection→analysis pipeline is demo-ready.

---

## Pre-Flight Checks

1. **Verify VPS Health**
   ```bash
   curl -s http://187.124.45.161:8000/api/v1/system/health | python3 -m json.tool
   ```
   - All components must show "healthy" or "active"

2. **Verify Frontend Dev Server**
   ```bash
   cd frontend && npm run dev
   ```
   - Must be running on http://localhost:3000

3. **Verify Alert Data Exists**
   ```bash
   curl -s "http://187.124.45.161:8000/api/v1/alerts/?limit=5&sort=desc" | python3 -m json.tool
   ```
   - Should show recent alerts including port_scan from Phase 2

---

## Task 1: Alert Console E2E Verification

### 1.1 Page Load Verification
- [ ] Navigate to http://localhost:3000/alerts
- [ ] Verify page loads without errors
- [ ] Verify alert table renders with data
- [ ] Check browser console for errors (F12 → Console)

### 1.2 Filter Functionality
- [ ] Test severity filters: all → critical → high → medium → low
- [ ] Test status filters: all → open → acknowledged → resolved
- [ ] Test category filters: all → port_scan → ddos → anomaly
- [ ] Verify filters work in combination

### 1.3 Alert Detail Verification
- [ ] Click on a port_scan alert from Phase 2 attack
- [ ] Verify AlertDetailDrawer opens
- [ ] Check all fields populate correctly:
  - [ ] alert_id (UUID format)
  - [ ] severity badge (MEDIUM expected for port_scan)
  - [ ] category ("port_scan")
  - [ ] title (descriptive)
  - [ ] source_ip and dest_ip
  - [ ] confidence score (52% expected from Phase 2)
  - [ ] created_at timestamp
  - [ ] status (should be "open")
  - [ ] **ai_narrative** — MUST be populated (not null/empty)
  - [ ] ai_playbook — if available
  - [ ] Related flows section
  - [ ] IOC enrichment section

### 1.4 API Endpoint Verification
```bash
# Get specific alert with IOC enrichment
curl -s "http://187.124.45.161:8000/api/v1/alerts/{alert_id}" | python3 -m json.tool
```
- [ ] Response includes `ai_narrative` field with content
- [ ] Response includes `ioc_matches` array
- [ ] Response includes related flow data

### 1.5 Alert Status Update
- [ ] Test status change: open → acknowledged
- [ ] Verify status persists after refresh
- [ ] Test status change: acknowledged → resolved
- [ ] Verify resolution_note field works

---

## Task 2: AI Analyst E2E Verification

### 2.1 Page Load Verification
- [ ] Navigate to http://localhost:3000/ai-analyst
- [ ] Verify chat interface loads
- [ ] Verify quick actions panel renders
- [ ] Verify token budget display shows correctly
- [ ] Check browser console for errors

### 2.2 Context Loading
- [ ] Test loading with alert context: http://localhost:3000/ai-analyst?alert_id={alert_id}
- [ ] Verify context selector shows alert info
- [ ] Verify context panel displays alert data

### 2.3 Streaming Chat Test
- [ ] Send query: "Explain the latest port scan alert"
- [ ] Verify message sends successfully
- [ ] Observe streaming response (typing effect)
- [ ] Record response time (T3 - T2)
- [ ] Verify response completes without errors

### 2.4 Response Quality Assessment
Evaluate AI response on:
- [ ] Technical accuracy (mentions SYN scan, ports, nmap)
- [ ] No hallucinations or false claims
- [ ] Clear, professional language
- [ ] Actionable recommendations included
- [ ] Quality rating: ___/5

### 2.5 Quick Actions Test
- [ ] Test "Analyze Latest Alert" button
- [ ] Test "Check IP Reputation" button
- [ ] Test "Model Performance" button
- [ ] Test "Recent Anomalies" button
- [ ] Verify each produces relevant response

### 2.6 API Endpoint Verification
```bash
# Test streaming chat endpoint
curl -X POST "http://187.124.45.161:8000/api/v1/llm/chat" \
  -H "Content-Type: application/json" \
  -d '{"messages": [{"role": "user", "content": "Summarize today's threats"}], "task_type": "general_analysis"}'
```
- [ ] Verify SSE streaming works
- [ ] Verify response format is correct

---

## Task 3: WebSocket Connection Verification

### 3.1 Connection Status
- [ ] Open browser DevTools → Network tab
- [ ] Filter by WS (WebSocket)
- [ ] Verify connection to ws://187.124.45.161:8000/api/v1/ws/
- [ ] Check connection status shows "Connected"

### 3.2 Real-Time Alert Push
- [ ] Keep /alerts page open
- [ ] Trigger a new alert (run port scan or use PCAP)
- [ ] Verify new alert appears without page refresh
- [ ] Record latency from alert creation to UI appearance

### 3.3 Connection Resilience
- [ ] Check reconnection logic in useWebSocket.ts
- [ ] Verify heartbeat mechanism (30s interval)
- [ ] Verify connection polling (2s interval)

---

## Task 4: Remaining Phase 2 Tasks

### 4.1 Detection Latency Logging
**File:** `backend/ml/inference/worker.py`
- [ ] Add timestamp when flow is received from Redis
- [ ] Add timestamp when ML scoring completes
- [ ] Add timestamp when alert is created
- [ ] Calculate and log latency: flow_reception → alert_creation
- [ ] Publish latency metrics to Redis for frontend display

### 4.2 Diverse Attack Simulation
**Scripts:** `scripts/attack_simulation/`
- [ ] Run `02_ddos_simulation.sh` — verify CRITICAL alert
- [ ] Run `03_dns_tunnel.py` — verify MEDIUM alert, category "dns_tunnel"
- [ ] Run `04_brute_force.sh` — verify HIGH alert, category "brute_force"
- [ ] Run `05_normal_traffic.sh` — verify ZERO false positives
- [ ] Document results for each attack type

### 4.3 Redis Security Hardening
**File:** `docker-compose.yml`
- [x] Verify Redis bound to 127.0.0.1 only: `"127.0.0.1:6379:6379"`
- [x] Confirmed already implemented in current docker-compose.yml
- [ ] Verify no external access: `docker compose exec redis redis-cli INFO replication`

---

## Success Criteria

| Criterion | Target | Actual | Pass/Fail |
|-----------|--------|--------|-----------|
| Alert Console loads | Yes | ___ | ___ |
| Alert detail shows AI narrative | Populated | ___ | ___ |
| AI Analyst page loads | Yes | ___ | ___ |
| Streaming chat works | SSE streaming | ___ | ___ |
| Response latency | <10s | ___s | ___ |
| Response quality | ≥4/5 | ___/5 | ___ |
| WebSocket connection | Connected | ___ | ___ |
| Real-time alert push | <5s latency | ___s | ___ |
| DDoS simulation | CRITICAL alert | ___ | ___ |
| DNS tunnel simulation | MEDIUM alert | ___ | ___ |
| Brute force simulation | HIGH alert | ___ | ___ |
| Normal traffic | ZERO alerts | ___ | ___ |

---

## Execution Order

1. Pre-flight checks (5 min)
2. Alert Console verification (15 min)
3. AI Analyst verification (15 min)
4. WebSocket testing (10 min)
5. Diverse attack simulation (30 min)
6. Detection latency logging implementation (20 min)
7. Documentation and reporting (15 min)

**Estimated Total:** ~110 minutes

---

## Risk Mitigation

| Risk | Mitigation |
|------|-----------|
| AI narrative not populated | Run `/api/v1/llm/analyze-alert/{id}` manually |
| Streaming fails | Check LLM Gateway logs, verify OpenRouter connectivity |
| WebSocket not connecting | Check DEV_MODE settings, verify token in localStorage |
| Attack scripts fail | Use PCAP upload fallback from `pcaps/demo/` |
| VPS components idle | `docker-compose restart` on affected services |

---

_Phase 3 Execution Plan — Alert Console + AI Analyst E2E Testing_
_Target: v0.6.2 with verified E2E detection→analysis pipeline_
